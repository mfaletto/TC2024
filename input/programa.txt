int suma(int a, int b) {
    return a + b;
}

int main() {

    int x = 10;
    int y = 20;
    double z = 30.5;

    int resultado;

    x = x + 1;
    y = y - 2;
    z = z * 2.5;


    if (x < y) {
        x = x + 5;
    } else {
        x = x - 5;
    }


    for (int i = 0; i < 10; i++) {
        x = x + i;
    }


    while (x > 0) {
        x = x - 1;
    }

     resultado = suma(x, y);


    return 0;
}